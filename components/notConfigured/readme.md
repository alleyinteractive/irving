Default component that will display if a component `name` string is provided, but does not have a corresponding key/entry in `config/componentMap.js`.

```js
<NotConfigured name="component-name">Test</NotConfigured>
```
<Meta title="Features|Available Packages" />

# Available Packages

## Core
* `core` package
* `babel-preset-irving` package
* `forms` package
* `integrations` package

## Styling
Irving is styling agnostic, but we offer some packages to make your life easy.

### Styled Components
* `styled` package
* `styled-components` package

### PostCSS
* `postcss` package

## Functionaality
* `audio-player` package

## WordPress
* `wp-admin-bar` package

## Hosting
* `aws-lambda` package
* `vip-go` package
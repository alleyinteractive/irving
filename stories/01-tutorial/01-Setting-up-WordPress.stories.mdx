<Meta title="Tutorial|Setting up WordPress" />

# Setting up WordPress
In this section, we'll prepare a WordPress installation to begin working on your Irving application.

To get started, you'll need a local copy of WordPress. The WordPress documentation on [how to install WordPress](https://wordpress.org/support/article/how-to-install-wordpress/) is a good resource to get started.

## Table of Contents
* [Install and activate the WP Irving plugin](?path=/docs/tutorial-setting-up-wordpress--page#install-and-activate-the-wp-irving-plugin)
* [Update `API_ROOT_URL` in `.env`](?path=/docs/tutorial-setting-up-wordpress--page#update-api_root_url-in-env)
* [Update Site Address in WordPress](?path=/docs/tutorial-setting-up-wordpress--page#update-site-address-in-wordpress)
* [Confirm your Changes](?path=/docs/tutorial-setting-up-wordpress--page#confirm-your-changes)
* [Scaffold Template JSON](?path=/docs/tutorial-setting-up-wordpress--page#scaffold-template-json)
* [Up Next](?path=/docs/tutorial-setting-up-wordpress--page#up-next)

## Install and activate the WP Irving plugin
Next, we want to download, install, and activate the [WP Irving](https://github.com/alleyinteractive/wp-irving) plugin on your local installation.

You should immediately see a new "Irving Development" dropdown in your [admin bar](http://localhost:6006/?path=/docs/packages-wordpress--page#admin-bar).

<img src="/assets/img/tutorial/01/admin-bar.png" style={{maxWidth: "600px", margin: "auto", width: "100%", display: "flex"}} />

You can confirm the endpoint is setup correctly by visiting `yourdomain.com/wp-json/irving/v1` and confirming a valid response,
```json
  {
  "namespace": "irving/v1",
  "routes": {
    "/irving/v1": {
      "namespace": "irving/v1",
      "methods": [...],
      "endpoints": [...],
      "_links": {...}
    },
    "/irving/v1/components": {...},
    "/irving/v1/registered-components": {...},
    "/irving/v1/purge-cache": {...}
  },
  "_links": {...}
  }
```


If you get a `rest_no_route` error, double check your plugin is activated and [pretty permalinks](https://wordpress.org/support/article/using-permalinks/) are enabled.

```json
{
  "code": "rest_no_route",
  "message": "No route was found matching the URL and request method",
  "data": {
    "status": 404
  }
}
```

## Update `API_ROOT_URL` in `.env`
Now that we've confirmed WP Irving is setup and ready, replace `API_ROOT_URL` in your `.env`,

```bash
API_ROOT_URL=https://your-local-wp-site.com/wp-json/irving/v1
```

_You'll need to restart Irving if it's already running._

## Update Site Address in WordPress
Inside of WordPress, you'll need to [update the Site Address](https://wordpress.org/support/article/changing-the-site-url/) to the Irving application.

Default value is `http://localhost:3001`

<img src="/assets/img/tutorial/01/site-address.png" style={{maxWidth: "600px", margin: "auto", width: "100%", display: "flex"}} />

## Confirm your Changes
Run `npm run dev` and you should see a "welcome" message.

<img src="/assets/img/tutorial/01/welcome.png" style={{maxWidth: "600px", margin: "auto", width: "100%", display: "flex"}} />

## Scaffold Template JSON
One of the ways WP Irving helps streamline development is by offering a basic templating system, mimicking the [WordPress Template Hierarchy](https://developer.wordpress.org/themes/basics/template-hierarchy/).

By default, the [Components Endpoint](?path=/docs/components-components-endpoint--page) will be constructed for any given request using `.json` files in `/wp-content/your-theme/templates/` and `/wp-content/your-theme/template-parts/`.

To get you started, the [Irving Starter Theme](https://github.com/alleyinteractive/irving-starter-theme) contains a simple Irving example.

We recommend this initial scaffolding,

```
wp-content
│
└───your-theme
    │   functions.php
    │   index.php
    │   style.css
    │
    └───templates
    │       archive.json
    │       author.json
    │       defaults.json
    │       frontpage.json
    │       index.json
    │       page.json
    │       single.json
    │
    └───template-parts
            header.json
            footer.json
```

## Up Next
We'll create a `/templates/single.json` file in our WordPress theme to lay out the template used by posts.

[Next Up: Adding a Template](?path=/docs/tutorial-adding-a-template--page)

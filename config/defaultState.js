export const components = [];
export const route = {
  action: '',
  location: {},
  siteComponents: [],
  pages: {},
};

const defaultState = {
  components,
  route,
};

export default defaultState;

export const components = [];
export const route = {
  notFound: false,
  action: '',
  location: {},
  siteComponents: [],
  pages: {},
};

const defaultState = {
  components,
  route,
};

export default defaultState;

export const components = {
  defaults: [],
  page: {},
};

export const route = {
  status: false,
  redirectTo: false,
  action: '',
};

const defaultState = {
  components,
  route,
};

export default defaultState;

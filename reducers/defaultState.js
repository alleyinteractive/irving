export const components = {
  global: {},
};

export const route = {
  notFound: false,
  action: '',
  location: {},
};

const defaultState = {
  components,
  route,
};

export default defaultState;

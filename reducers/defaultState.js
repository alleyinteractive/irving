export const components = {
  defaults: [],
  page: {},
};

export const route = {
  status: null,
  redirectTo: false,
  action: '',
};

export const error = null;

const defaultState = {
  components,
  route,
  error,
};

export default defaultState;

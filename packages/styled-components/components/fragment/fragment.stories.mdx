import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';
import Fragment from './fragment'

<Meta title="Irving-Common/Fragment" component={Fragment} />

# Fragment
Render children in a React fragment or any other HTML tag.

Documentation on fragments in React, https://reactjs.org/docs/fragments.html

## Props
// @todo how should this actually be formatted?
string $tag Optional. Render children in a node with this tag. Defaults to an empty string.

<Preview>
  <Story name="Fragment">
    <Fragment tag="section">Example fragment</Fragment>
  </Story>
</Preview>

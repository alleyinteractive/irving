import { css } from 'styled-components';

/* eslint-disable max-len */
const link = css`
  color: inherit;
  font-weight: inherit;
  position: relative;
  text-decoration: underline;

  &:hover {
    opacity: 0.5;
  }
`;

export default link;

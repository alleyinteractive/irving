import React from 'react';

const DataPlaceholder = () => (
  <div
    style={{
      backgroundColor: '#EEEEEE',
      height: '100%',
      width: '100%',
    }}
  />
);

export default DataPlaceholder;

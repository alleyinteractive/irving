/**
 * A components request that expects only the related components for that particular page.
 */
module.exports.CONTEXT_PAGE = 'page';

/**
 * A components request that expects all components for the page, including the global defaults.
 */
module.exports.CONTEXT_SITE = 'site';
